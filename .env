# Database configuration
# This file contains the database connection details for the AttendanceApp.
# Make sure to replace the values with your actual database credentials.
DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=AttendanceApp
